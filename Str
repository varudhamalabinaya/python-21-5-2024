n = int(input("Enter the value for n: "))
k = int(input("Enter the value for k : "))
if k < 2 or k > 9:
    print("Invalid value ")
else:
    count = 0
    num = 1
    while count < n:
        if str(k) in str(num) or num % k == 0:
            count += 1
        num += 1
    print(num - 1)
